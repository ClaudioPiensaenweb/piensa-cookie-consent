# Dependencies
/vendor/
/node_modules/

# Build artefacts
/build/
/dist/
*.zip

# Tooling caches
.phpcs-cache
.phpunit.result.cache
/.wp-env.json
/.cache/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Local env
.env
.env.*
!.env.example

# Generated translations (kept out of VCS, built on release)
# Compiled catalogues are committed: WordPress loads the .mo and not the .po,
# and the release is built from the working tree, so ignoring them shipped
# packages with no translations at all.

# Session logs written by local tooling, not part of the plugin.
.planning/
